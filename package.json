{
  "name": "discord-lookup",
  "version": "1.0.0",
  "description": "discord user id lookup",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SANAGO",
  "license": "ISC",
  "dependencies": {
    "prompt-sync": "^4.2.0"
  }
}
